<h1>PerfilDocente creation</h1>

<form action="{{ path('admincloud_perfildocente_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_perfildocente') }}">
            Back to the list
        </a>
    </li>
</ul>
