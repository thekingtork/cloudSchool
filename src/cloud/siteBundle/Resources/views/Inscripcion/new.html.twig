<h1>Inscripcion creation</h1>

<form action="{{ path('admincloud_inscripcion_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_inscripcion') }}">
            Back to the list
        </a>
    </li>
</ul>
