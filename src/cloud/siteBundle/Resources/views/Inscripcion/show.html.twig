<h1>Inscripcion</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Inscripcion_inicio</th>
            <td>{{ entity.inscripcioninicio|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Inscripcion_final</th>
            <td>{{ entity.inscripcionfinal|date('Y-m-d H:i:s') }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_inscripcion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admincloud_inscripcion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('admincloud_inscripcion_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
