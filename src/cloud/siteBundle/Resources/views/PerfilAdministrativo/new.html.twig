<h1>PerfilAdministrativo creation</h1>

<form action="{{ path('admincloud_perfiladministrativo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_perfiladministrativo') }}">
            Back to the list
        </a>
    </li>
</ul>
