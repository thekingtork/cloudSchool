<h1>Institucion</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Direccion</th>
            <td>{{ entity.direccion }}</td>
        </tr>
        <tr>
            <th>Ciudad</th>
            <td>{{ entity.ciudad }}</td>
        </tr>
        <tr>
            <th>Telefono</th>
            <td>{{ entity.telefono }}</td>
        </tr>
        <tr>
            <th>Fax</th>
            <td>{{ entity.fax }}</td>
        </tr>
        <tr>
            <th>Nit</th>
            <td>{{ entity.nit }}</td>
        </tr>
        <tr>
            <th>Dane</th>
            <td>{{ entity.dane }}</td>
        </tr>
        <tr>
            <th>Men</th>
            <td>{{ entity.men }}</td>
        </tr>
        <tr>
            <th>Rector</th>
            <td>{{ entity.rector }}</td>
        </tr>
        <tr>
            <th>Secretario</th>
            <td>{{ entity.secretario }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ entity.email }}</td>
        </tr>
        <tr>
            <th>Distribucion_usuario</th>
            <td>{{ entity.distribucionusuario }}</td>
        </tr>
        <tr>
            <th>Modelo_pedagogico</th>
            <td>{{ entity.modelopedagogico }}</td>
        </tr>
        <tr>
            <th>Url_imagen</th>
            <td>{{ entity.urlimagen }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_institucion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admincloud_institucion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('admincloud_institucion_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
