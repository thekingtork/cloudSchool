<h1>ActividadTipo creation</h1>

<form action="{{ path('admincloud_actividadtipo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_actividadtipo') }}">
            Back to the list
        </a>
    </li>
</ul>
