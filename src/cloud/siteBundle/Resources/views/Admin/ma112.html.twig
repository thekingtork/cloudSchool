{% extends app.request.isXmlHttpRequest ? "cloudBundle::layout_ajax.html.twig" : "cloudBundle::layout.html.twig" %} 
{% block title %}
	MA112
{% endblock %}
{% block menu_principal %}
		<div id="ContAjax">
			<link rel="stylesheet" href="{{ asset('stylesheets/Ma1/Ma112/ma112.css') }}"/>
			<div id="bloqueCgenerales">
			  	<div id="contenidoIzq">
					<div class="botonRegresar">
						{% block back %}
							<a href="{{ path('ma11') }}" class="ajax">
								<img src="{{ asset('images/b_atras_blue.png') }}">
							</a>
						{% endblock %}
					</div>
					<a href="{{ path('ma112_new') }}">
						<div id="addSede">
							<img src="{{ asset('images/b_agregar.png') }}" alt=""/>
							<p>agregar Sede</p>
						</div>				
					</a>
					<div class="InicioBlue" ><a href="{{ path('ma') }}" class="ajax" >Inicio</a></div>
				</div>
				<div id="contenidoDer">
					<div class="breadcumbrBlue">
						<a href="{{ path('ma1') }}" class="ajax">Ajustes del sistema</a>
					</div>
					<div id="TituloSecction">
						<label>Administraci&oacute;n de Sedes</label>
					</div>
					{% if mje is defined %}
						<div>
							<p>{{ mje }}</p>
						</div>
					{% else %}
						<div id="content_1" class="mCustomScrollbar _mCS_1">
							    {% for sede in sedes %}
										<div class="sede">
					                        <a href="{{ path('ma112_edit',{'id' : sede.id }) }}" class="ajax">
													<p class="signoUp">+</p>
													<p class="titulocajas">{{sede.name}}</p>
											</a>
										</div>
			                    {% endfor %}
						</div>
		            {% endif %}
				</div>
			</div>
		</div>
    <span style="display: none;" id="enlace" data-num="{{numero|default('')}}"></span>
{% endblock %}