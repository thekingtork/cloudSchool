<h1>CodigoVerificacion creation</h1>

<form action="{{ path('admincloud_codigoverificacion_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_codigoverificacion') }}">
            Back to the list
        </a>
    </li>
</ul>
