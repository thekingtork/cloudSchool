<h1>Periodo creation</h1>

<form action="{{ path('admincloud_periodo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admincloud_periodo') }}">
            Back to the list
        </a>
    </li>
</ul>
